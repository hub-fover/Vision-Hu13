export function forwardBackwardError(forward,backward){return Math.hypot(forward[0]-backward[0],forward[1]-backward[1]);}
export function trackFlowSequence(motions){return motions.map((motion,index)=>({frameIndex:index,timeS:index/30,dxPx:Number(motion.offsetX||0)-Number(motions[0].offsetX||0),dyPx:Number(motion.offsetY||0)-Number(motions[0].offsetY||0),score:Number(motion.score||.92),valid:Number(motion.score||.92)>=.55,errorCode:null}));}
