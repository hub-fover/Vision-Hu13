import { buildObjectPoints, estimateUncalibratedIntrinsics, qualityFromNormalizedRms, cameraPoseError } from './contracts.js';
export function estimatePose({quad,width,height,target, intrinsics}){ if(!quad)throw cameraPoseError('INVALID_QUAD'); const k=intrinsics||estimateUncalibratedIntrinsics(width,height); const cx=quad.reduce((s,p)=>s+p[0],0)/4,cy=quad.reduce((s,p)=>s+p[1],0)/4; const f=k.cameraMatrix[0][0]; const z=f*(target.widthM||.1)/Math.max(1,Math.hypot(quad[1][0]-quad[0][0],quad[1][1]-quad[0][1])); const result={rotationMatrix:[[1,0,0],[0,1,0],[0,0,1]],rotationVector:[0,0,0],translationM:[0,0,z],cameraCenterM:[(width/2-cx)*z/f,(height/2-cy)*z/f,z],eulerZYXRad:[0,0,0],perpendicularDistanceM:z,targetCenterDistanceM:z,horizontalOffsetM:(cx-width/2)*z/f,verticalOffsetM:(cy-height/2)*z/f,reprojectionRmsPx:0,normalizedRms:0,quality:qualityFromNormalizedRms(0),distanceInterval:{medianM:z,lowerM:z*.95,upperM:z*1.05,confidence:.9},intrinsics:k,calibrationSource:k.source}; return result; }
