# OpenCV.js runtime

Run `npm run vendor:opencv` after installing web dependencies. The generated
`opencv.js` is same-origin, ignored by Git, and loaded only when enhanced
checkerboard calibration is requested.
