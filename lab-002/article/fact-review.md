# 《几张照片，怎样接成一张？》事实审校

审校日期：2026-07-30
对象：`article.md`、`claims.yaml` 与 `sources.yaml`

## 核对结果

- 算法链路只描述当前仓库已实现的相邻图匹配、ORB、BF-Hamming KNN、0.75 比率筛选、双向一致性、RANSAC、中间锚点、曝光、羽化和裁剪；参数来自共享契约，外部方法背景链接到 OpenCV 官方资料。
- 山景、城市、海面三组素材的作者、作品页、许可证、帧时间戳和派生方法均与 `asset-manifest.json` 一致。正文没有把第三方帧或技术图底图声明为原创或 CC BY 4.0。
- 海面结论按诊断文件原样收窄：完整三帧序列在该次运行中成功；海面 ROI 压力测试只有 9 组比率匹配并返回 `INSUFFICIENT_OVERLAP`。未把近景视差或移动物体重影写成已有图像证据。
- 隐私表述限定为当前仓库应用代码和自动化检查。页面会读取同站点静态示例及 OpenCV 文件，但不会发送用户选择的图片。
- `capture="environment"` 只写成相机方向提示，并明确移动浏览器支持不完全一致；普通相册入口保留。
- 真机演示状态仍是 `PENDING_DEVICE_CAPTURE`。正文与公众号 HTML 均未引用 GIF、MP4、WebM 或模拟手机素材。
- 未发现作者亲历、朋友同事、采访、对话、具体现场动作或感官细节。

## 来源可达性说明

OpenCV 官方页面若在某些网络环境返回访问限制，本文不引用其原文句子；关于当前产品行为和具体数值的陈述均以仓库契约、实现、测试和真实诊断数据为直接证据。

结论：正文中的事实、推断和意见已分开记录，无待补来源的具体主张。
